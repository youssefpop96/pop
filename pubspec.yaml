name: pop
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0-113.1.beta

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^8.1.3
  supabase_flutter: ^2.12.0
  google_sign_in: 6.2.1
  image_picker: ^1.2.1
  flutter_markdown: ^0.7.7+1
  pdf: ^3.12.0
  printing: ^5.14.3
  flutter_localizations:
    sdk: flutter
  flutter_html: ^3.0.0
  flutter_quill: ^11.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/

